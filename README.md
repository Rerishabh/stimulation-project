# simulation-project
call center 

A C-based call centre simulation that uses queues to manage incoming calls and assigns them to available agents. The system tracks call duration, agent availability, and processes events in time steps to mimic real call-handling operations.
